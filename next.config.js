/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the output: 'export' line if it exists
  // output: 'export',
  experimental: {
    // Remove or comment out this line
    // appDir: true,
    
    // Keep any other experimental options you might have
  },
}

module.exports = nextConfig
